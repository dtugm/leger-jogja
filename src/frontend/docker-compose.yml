services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: leger-jogja-frontend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_BASE_URL=${NEXT_PUBLIC_API_BASE_URL:-http://localhost:8000}
    restart: always
