import AuthLayout from "@/components/auth/auth-layout";
import FormNewPassword from "@/components/auth/form-new-password";

export default function NewPasswordPage() {
  return <AuthLayout><FormNewPassword /></AuthLayout>;
}